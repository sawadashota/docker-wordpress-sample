Docker WordPress Sample
===

* Docker Compose
* Original Theme With Webpack

Getting Started
---

```
$ docker-compose pull
$ docker-compose run --rm node yarn install
$ docker-compose up -d
```

Access [http://localhost:9000/](http://localhost:9000/)

Build
---

```
$ docker-compose run --rm node yarn build
```
